import React from 'react'; // A estruturação da plataforma - "O framework"
import ReactDOM from 'react-dom'; // A biblioteca que permite trabalhar com o React no navegador (No DOM)

import App from './App';

ReactDOM.render(<App />, document.getElementById('root'));
